<html>
<body>
<% if current_user && current_user.admin %>
<h1>Product Management</h1><br/>
<table>
    <tr class="top-row">
      <th>Product Name</th>
      <th>Description</th>
      <th class="money">Price</th>
      <th>Categories</th>
      <th>Options</th>
    </tr>
<% @products.each_with_index do |product, index| %>
    <tr class="content">
      <td class="name"><%= product.title %></td>
      <td><%= product.description %></td>
      <td class="money"><%= number_to_currency(product.price, :unit => "$") %></td>
      <td><% product.categories.uniq.each do |category| %>
        <%=category.name %>
        <%end%></td>
      <td class="link"><%= link_to 'Edit', edit_product_path(product), :class => "button" %></td>
      <td><% if product.retired == true %>
        (retired)
        <%end%></td>
    </tr>
  <%end%>
</table>

<%else%>

    <div class="container-fluid">
    <div class="row-fluid">
      <%= render :partial => '../views/categories', :locals => {:categories => @categories} %>

      <div class="span9">
        <div class="hero-unit">
          <h1>Stolen Goods!</h1>
          <p>We steal shit.  Straight up.  That means our costs are just about non-existant, and we pass those savings on to you, our customer.  Be carefull though, if we see you on the street with anything you bought here, we're likely to rob the shit out of you!!</p>
          <p><a href="#" class="btn btn-primary btn-large">Learn more »</a></p>
        </div>

        <div class="row-fluid">
          <% @products.each do |product| %>
             <div class="span4">
              <h2><%= product.title %></h2>
              <p><%= product.description %></p>
              <p><%= product.price %><p>
              <p> <%= link_to "View details »", product_path(product), class: "btn" %></p>
             </div>
          <% end %>
        </div>
    </div>
<%end%>
</body>
</html>




