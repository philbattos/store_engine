<div class="span8" >
  <h1><%= @product.title %></h1>

  <p>
    <%= @product.description %>
  </p>
  <p>
    <%= @product.price %>
  </p>

  <%= link_to "Add to cart", cart_path(increase: 1, product_id: @product.id), method: :put %>
</div>

<%= render '../views/mini_cart' %>